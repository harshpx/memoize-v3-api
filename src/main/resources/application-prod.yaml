spring:
#  # Redundant but explicit activation of the 'dev' profile
#  # Will automatically apply when correct naming convention is used
#  config:
#    activate:
#      on-profile: dev
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: false
logging:
  level:
    root: WARN
env: prod